services:
  agent:
    volumes:
      - type: bind
        source: ${VARVIK_CODEX_AUTH_FILE:?set VARVIK_CODEX_AUTH_FILE}
        target: /run/secrets/varvik-codex-auth.json
        read_only: true
  agent-engineering:
    volumes:
      - type: bind
        source: ${VARVIK_CODEX_AUTH_FILE:?set VARVIK_CODEX_AUTH_FILE}
        target: /run/secrets/varvik-codex-auth.json
        read_only: true
  agent-creative:
    volumes:
      - type: bind
        source: ${VARVIK_CODEX_AUTH_FILE:?set VARVIK_CODEX_AUTH_FILE}
        target: /run/secrets/varvik-codex-auth.json
        read_only: true
  agent-research:
    volumes:
      - type: bind
        source: ${VARVIK_CODEX_AUTH_FILE:?set VARVIK_CODEX_AUTH_FILE}
        target: /run/secrets/varvik-codex-auth.json
        read_only: true
